# Bookrithm
Simulador de algoritmos e estruturas de dados.

#build
  git add .
  git commit -m "test github actions"
  git push -u  origin

#Acesso
Pode ser feito o download do binário em releases, e também pode ser acessado via https://yazuc.github.io/
